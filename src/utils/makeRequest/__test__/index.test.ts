export {}

jest.mock('axios');

describe('makeRequest', () => {
  xit('should make api call when no options are provided and api endpoint is provided', async () => {
    return;
  });

  xit('should make api call when options are provided to update claps and api endpoint is provided', async () => {
    return;
  });
});